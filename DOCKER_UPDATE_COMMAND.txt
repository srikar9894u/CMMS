╔══════════════════════════════════════════════════════════════════════╗
║              CMD FOR DOCKER UPDATE FRESH                              ║
╚══════════════════════════════════════════════════════════════════════╝

EASIEST WAY - RUN THE BATCH FILE:
═══════════════════════════════════════════════════════════════════════

  cd C:\Users\Srikar.Tanukula\CMMS
  update-docker.bat

That's it! The script does everything automatically.


MANUAL COMMANDS - FRESH REBUILD:
═══════════════════════════════════════════════════════════════════════

  cd C:\Users\Srikar.Tanukula\CMMS
  git pull origin claude/build-cmms-webapp-011CUMdU5DjLj8D3AJLooDTr
  docker compose down
  docker compose pull
  docker compose build --no-cache
  docker compose up -d


AFTER UPDATE - CHECK STATUS:
═══════════════════════════════════════════════════════════════════════

  docker compose ps
  curl http://localhost:3000/health


AFTER UPDATE - CONFIGURE S7-416 PLC:
═══════════════════════════════════════════════════════════════════════

  cd opc-service
  python setup_s7_connection.py
  python setup_s7_tags.py
  python s7_service.py


TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════════════

If update fails:
  1. Check Docker Desktop is running
  2. Run: docker compose logs
  3. Try: docker system prune -a
  4. Run update-docker.bat again


QUICK COMMANDS:
═══════════════════════════════════════════════════════════════════════

  Start:    docker compose up -d
  Stop:     docker compose down
  Restart:  docker compose restart
  Logs:     docker compose logs -f
  Status:   docker compose ps

